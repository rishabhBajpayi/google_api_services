name: google_api_services
description: A helper function to use google api services
version: 0.0.1
repository: https://github.com/rishabhBajpayi/google_api_services.git

environment:
  sdk: '>=2.18.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
